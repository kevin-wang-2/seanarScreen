{
  "name": "seanarscreen",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ seanarscreen --out ./StPipe-electron --overwrite --icon=./StPipe.ico --all --asar"
  },
  "dependencies": {
    "electron-packager": "latest"
  },
  "devDependencies": {
    "electron": "latest",
    "ini": "latest"
  }
}
